server:
  port: ${PORT:8080}
  address: 0.0.0.0

spring:
  application:
    name: ecommerce

  datasource:
    url: postgresql://postgres:Supabase2025DbPass@db.qegvujunggqjtvvjrehm.supabase.co:5432/postgres
    username: postgres
    password: Supabase2025DbPass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    open-in-view: false

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect














